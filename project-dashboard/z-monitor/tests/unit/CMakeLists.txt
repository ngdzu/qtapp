# Unit tests

# Mocks library (for use in tests)
add_subdirectory(../mocks)

# Domain layer tests
add_subdirectory(domain)

# Logging unit tests
add_subdirectory(logging)

# Core service tests
add_subdirectory(core)

