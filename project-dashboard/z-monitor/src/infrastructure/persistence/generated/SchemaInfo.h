/**
 * @file SchemaInfo.h
 * @brief Auto-generated database schema constants.
 * 
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * 
 * This file is auto-generated from schema/database.yaml
 * To make changes:
 *   1. Edit schema/database.yaml
 *   2. Run: python3 scripts/generate_schema.py
 *   3. Commit both files together
 * 
 * Generated: 2025-12-01 21:53:26 UTC
 * Schema Version: 1.0.0
 */

#ifndef SCHEMAINFO_H
#define SCHEMAINFO_H

#include <string>

/**
 * @namespace Schema
 * @brief Database schema constants (table names, column names, constraints).
 * 
 * Use these constants instead of hardcoded strings to ensure type safety
 * and enable refactoring.
 */
namespace Schema {

/**
 * @namespace Tables
 * @brief Table name constants.
 */
namespace Tables {
    ///< Immutable audit trail for all user actions with hash chain for tamper detection
    constexpr const char* ACTION_LOG = "action_log";
    ///< Patient admission, discharge, and transfer events for audit and compliance
    constexpr const char* ADMISSION_EVENTS = "admission_events";
    ///< Alarm events and lifecycle metadata
    constexpr const char* ALARMS = "alarms";
    ///< User-added notes for specific snapshots or records
    constexpr const char* ANNOTATIONS = "annotations";
    ///< Tracks archival/export jobs and their status
    constexpr const char* ARCHIVAL_JOBS = "archival_jobs";
    ///< Certificate metadata for installed device/server certs (no private keys stored)
    constexpr const char* CERTIFICATES = "certificates";
    ///< Metadata about DB encryption key and algorithm (not the key itself)
    constexpr const char* DB_ENCRYPTION_META = "db_encryption_meta";
    ///< Device health and telemetry events
    constexpr const char* DEVICE_EVENTS = "device_events";
    ///< Detailed infusion pump logging (start/stop/pause/errors)
    constexpr const char* INFUSION_EVENTS = "infusion_events";
    ///< UI notifications history for NotificationBell and audit
    constexpr const char* NOTIFICATIONS = "notifications";
    ///< Patient demographic and admission information. Serves as both local patient registry and cache for external system lookups.
    constexpr const char* PATIENTS = "patients";
    ///< Predictive analytics outputs and provenance
    constexpr const char* PREDICTIVE_SCORES = "predictive_scores";
    ///< Security-relevant events for audit, forensics, and compliance with hash chain for tamper detection
    constexpr const char* SECURITY_AUDIT_LOG = "security_audit_log";
    ///< Device configuration settings and user preferences
    constexpr const char* SETTINGS = "settings";
    ///< Captured waveform or contextual snapshot data (blobs) with metadata
    constexpr const char* SNAPSHOTS = "snapshots";
    ///< Telemetry transmission timing and performance metrics for benchmarking and diagnostics
    constexpr const char* TELEMETRY_METRICS = "telemetry_metrics";
    ///< Local device accounts (PIN-based metadata) used for audit and RBAC
    constexpr const char* USERS = "users";
    ///< Vital signs time-series data. Largest table with high-frequency writes.
    constexpr const char* VITALS = "vitals";
}

/**
 * @namespace Columns
 * @brief Column name constants organized by table.
 */
namespace Columns {

    /**
     * @namespace ActionLog
     * @brief Column names for action_log table.
     *
     * Immutable audit trail for all user actions with hash chain for tamper detection
     */
    namespace ActionLog {
        ///< Action type (LOGIN, LOGOUT, AUTO_LOGOUT, ADMIT_PATIENT, etc.)
        constexpr const char* ACTION_TYPE = "action_type";
        ///< JSON string with additional context
        constexpr const char* DETAILS = "details";
        ///< Device identifier
        constexpr const char* DEVICE_ID = "device_id";
        ///< Error code if result is FAILURE
        constexpr const char* ERROR_CODE = "error_code";
        ///< Error message if result is FAILURE
        constexpr const char* ERROR_MESSAGE = "error_message";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< IP address (if available, for network actions)
        constexpr const char* IP_ADDRESS = "ip_address";
        ///< SHA-256 hash of previous entry (hash chain for tamper detection)
        constexpr const char* PREVIOUS_HASH = "previous_hash";
        ///< SUCCESS, FAILURE, PARTIAL
        constexpr const char* RESULT = "result";
        ///< SHA-256 hash of session token (for audit trail)
        constexpr const char* SESSION_TOKEN_HASH = "session_token_hash";
        ///< Target identifier (MRN, setting name, notification ID)
        constexpr const char* TARGET_ID = "target_id";
        ///< Type of target (PATIENT, SETTING, NOTIFICATION, etc.)
        constexpr const char* TARGET_TYPE = "target_type";
        ///< ISO 8601 timestamp for readability
        constexpr const char* TIMESTAMP_ISO = "timestamp_iso";
        ///< Unix timestamp in milliseconds
        constexpr const char* TIMESTAMP_MS = "timestamp_ms";
        ///< User who performed action (NULL if no login required)
        constexpr const char* USER_ID = "user_id";
        ///< User role (NURSE, PHYSICIAN, TECHNICIAN, ADMINISTRATOR)
        constexpr const char* USER_ROLE = "user_role";
    }

    /**
     * @namespace AdmissionEvents
     * @brief Column names for admission_events table.
     *
     * Patient admission, discharge, and transfer events for audit and compliance
     */
    namespace AdmissionEvents {
        ///< Source of admission (manual/barcode/central_station/transfer)
        constexpr const char* ADMISSION_SOURCE = "admission_source";
        ///< Bed/room location associated with the event
        constexpr const char* BED_LOCATION = "bed_location";
        ///< Additional event details (JSON format)
        constexpr const char* DETAILS = "details";
        ///< Device identifier/asset tag that performed the action
        constexpr const char* DEVICE_LABEL = "device_label";
        ///< Type of event (admission/discharge/transfer)
        constexpr const char* EVENT_TYPE = "event_type";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< Medical Record Number
        constexpr const char* PATIENT_MRN = "patient_mrn";
        ///< Patient name
        constexpr const char* PATIENT_NAME = "patient_name";
        ///< Unix milliseconds timestamp of event
        constexpr const char* TIMESTAMP = "timestamp";
        ///< Clinician/Technician who performed the action (NULL if automated)
        constexpr const char* USER_ID = "user_id";
    }

    /**
     * @namespace Alarms
     * @brief Column names for alarms table.
     *
     * Alarm events and lifecycle metadata
     */
    namespace Alarms {
        ///< User ID who acknowledged the alarm
        constexpr const char* ACKNOWLEDGED_BY = "acknowledged_by";
        ///< Unix milliseconds timestamp when alarm was acknowledged
        constexpr const char* ACKNOWLEDGED_TIME = "acknowledged_time";
        ///< Primary key
        constexpr const char* ALARM_ID = "alarm_id";
        ///< Type of alarm (HR_HIGH, SPO2_LOW, etc.)
        constexpr const char* ALARM_TYPE = "alarm_type";
        ///< Reference to snapshot table (if applicable)
        constexpr const char* CONTEXT_SNAPSHOT_ID = "context_snapshot_id";
        ///< Unix milliseconds timestamp when alarm ended
        constexpr const char* END_TIME = "end_time";
        ///< Patient identifier (deprecated, use patient_mrn)
        constexpr const char* PATIENT_ID = "patient_id";
        ///< Medical Record Number (REQUIRED for patient association)
        constexpr const char* PATIENT_MRN = "patient_mrn";
        ///< Alarm priority (CRITICAL/HIGH/MEDIUM/LOW)
        constexpr const char* PRIORITY = "priority";
        ///< Raw value that triggered the alarm
        constexpr const char* RAW_VALUE = "raw_value";
        ///< Unix milliseconds timestamp until which alarm is silenced
        constexpr const char* SILENCED_UNTIL = "silenced_until";
        ///< Unix milliseconds timestamp when alarm started
        constexpr const char* START_TIME = "start_time";
        ///< Alarm status (ACTIVE/ACKNOWLEDGED/SILENCED/CLEARED)
        constexpr const char* STATUS = "status";
        ///< Threshold that was exceeded (historical snapshot for audit)
        constexpr const char* THRESHOLD_VALUE = "threshold_value";
    }

    /**
     * @namespace Annotations
     * @brief Column names for annotations table.
     *
     * User-added notes for specific snapshots or records
     */
    namespace Annotations {
        ///< annotation_id
        constexpr const char* ANNOTATION_ID = "annotation_id";
        ///< author_id
        constexpr const char* AUTHOR_ID = "author_id";
        ///< creation_time
        constexpr const char* CREATION_TIME = "creation_time";
        ///< note
        constexpr const char* NOTE = "note";
        ///< sensitivity
        constexpr const char* SENSITIVITY = "sensitivity";
        ///< snapshot_id
        constexpr const char* SNAPSHOT_ID = "snapshot_id";
    }

    /**
     * @namespace ArchivalJobs
     * @brief Column names for archival_jobs table.
     *
     * Tracks archival/export jobs and their status
     */
    namespace ArchivalJobs {
        ///< archive_id
        constexpr const char* ARCHIVE_ID = "archive_id";
        ///< archive_location
        constexpr const char* ARCHIVE_LOCATION = "archive_location";
        ///< archived_at
        constexpr const char* ARCHIVED_AT = "archived_at";
        ///< archived_before_ts
        constexpr const char* ARCHIVED_BEFORE_TS = "archived_before_ts";
        ///< record_count
        constexpr const char* RECORD_COUNT = "record_count";
        ///< status
        constexpr const char* STATUS = "status";
        ///< table_name
        constexpr const char* TABLE_NAME = "table_name";
    }

    /**
     * @namespace Certificates
     * @brief Column names for certificates table.
     *
     * Certificate metadata for installed device/server certs (no private keys stored)
     */
    namespace Certificates {
        ///< SHA-256 fingerprint of certificate for quick lookup
        constexpr const char* CERT_FINGERPRINT = "cert_fingerprint";
        ///< Certificate issuer (CA information)
        constexpr const char* CERT_ISSUER = "cert_issuer";
        ///< Certificate serial number (unique identifier)
        constexpr const char* CERT_SERIAL = "cert_serial";
        ///< Certificate subject (CN, O, etc.)
        constexpr const char* CERT_SUBJECT = "cert_subject";
        ///< Unix milliseconds timestamp when record was created
        constexpr const char* CREATED_AT = "created_at";
        ///< Device identifier this certificate belongs to
        constexpr const char* DEVICE_ID = "device_id";
        ///< Unix milliseconds timestamp when certificate expires
        constexpr const char* EXPIRES_AT = "expires_at";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< Unix milliseconds timestamp when certificate was issued
        constexpr const char* ISSUED_AT = "issued_at";
        ///< Unix milliseconds timestamp of last successful certificate validation
        constexpr const char* LAST_VALIDATED_AT = "last_validated_at";
        ///< Reason for revocation if revoked (e.g., compromised, key_rotation)
        constexpr const char* REVOCATION_REASON = "revocation_reason";
        ///< Unix milliseconds timestamp when certificate was revoked
        constexpr const char* REVOKED_AT = "revoked_at";
        ///< Certificate status (active/expired/revoked/pending_renewal)
        constexpr const char* STATUS = "status";
    }

    /**
     * @namespace DbEncryptionMeta
     * @brief Column names for db_encryption_meta table.
     *
     * Metadata about DB encryption key and algorithm (not the key itself)
     */
    namespace DbEncryptionMeta {
        ///< algorithm
        constexpr const char* ALGORITHM = "algorithm";
        ///< created_at
        constexpr const char* CREATED_AT = "created_at";
        ///< key_version
        constexpr const char* KEY_VERSION = "key_version";
        ///< salt
        constexpr const char* SALT = "salt";
    }

    /**
     * @namespace DeviceEvents
     * @brief Column names for device_events table.
     *
     * Device health and telemetry events
     */
    namespace DeviceEvents {
        ///< battery_percent
        constexpr const char* BATTERY_PERCENT = "battery_percent";
        ///< cpu_temp_c
        constexpr const char* CPU_TEMP_C = "cpu_temp_c";
        ///< details
        constexpr const char* DETAILS = "details";
        ///< device_id
        constexpr const char* DEVICE_ID = "device_id";
        ///< event_type
        constexpr const char* EVENT_TYPE = "event_type";
        ///< id
        constexpr const char* ID = "id";
        ///< mem_percent
        constexpr const char* MEM_PERCENT = "mem_percent";
        ///< network_latency_ms
        constexpr const char* NETWORK_LATENCY_MS = "network_latency_ms";
        ///< timestamp
        constexpr const char* TIMESTAMP = "timestamp";
    }

    /**
     * @namespace InfusionEvents
     * @brief Column names for infusion_events table.
     *
     * Detailed infusion pump logging (start/stop/pause/errors)
     */
    namespace InfusionEvents {
        ///< drug_name
        constexpr const char* DRUG_NAME = "drug_name";
        ///< event_type
        constexpr const char* EVENT_TYPE = "event_type";
        ///< flow_rate_ml_per_hr
        constexpr const char* FLOW_RATE_ML_PER_HR = "flow_rate_ml_per_hr";
        ///< id
        constexpr const char* ID = "id";
        ///< is_synced
        constexpr const char* IS_SYNCED = "is_synced";
        ///< occlusion_pressure_mmHg
        constexpr const char* OCCLUSION_PRESSURE_MMHG = "occlusion_pressure_mmHg";
        ///< patient_id
        constexpr const char* PATIENT_ID = "patient_id";
        ///< time_remaining_sec
        constexpr const char* TIME_REMAINING_SEC = "time_remaining_sec";
        ///< timestamp
        constexpr const char* TIMESTAMP = "timestamp";
        ///< total_volume_infused_ml
        constexpr const char* TOTAL_VOLUME_INFUSED_ML = "total_volume_infused_ml";
    }

    /**
     * @namespace Notifications
     * @brief Column names for notifications table.
     *
     * UI notifications history for NotificationBell and audit
     */
    namespace Notifications {
        ///< expiration
        constexpr const char* EXPIRATION = "expiration";
        ///< id
        constexpr const char* ID = "id";
        ///< is_read
        constexpr const char* IS_READ = "is_read";
        ///< level
        constexpr const char* LEVEL = "level";
        ///< message
        constexpr const char* MESSAGE = "message";
        ///< related_alarm_id
        constexpr const char* RELATED_ALARM_ID = "related_alarm_id";
        ///< timestamp
        constexpr const char* TIMESTAMP = "timestamp";
    }

    /**
     * @namespace Patients
     * @brief Column names for patients table.
     *
     * Patient demographic and admission information. Serves as both local patient registry and cache for external system lookups.
     */
    namespace Patients {
        ///< Source of admission (Manual/Barcode/Central/Emergency)
        constexpr const char* ADMISSION_SOURCE = "admission_source";
        ///< Admission status (ADMITTED/DISCHARGED/TRANSFERRED)
        constexpr const char* ADMISSION_STATUS = "admission_status";
        ///< Unix milliseconds timestamp when patient was admitted to device (NULL if not currently admitted)
        constexpr const char* ADMITTED_AT = "admitted_at";
        ///< Known allergies (comma-separated or JSON)
        constexpr const char* ALLERGIES = "allergies";
        ///< Current bed/room assignment (e.g., ICU-4B) - part of ADT workflow
        constexpr const char* BED_LOCATION = "bed_location";
        ///< Unix milliseconds timestamp when patient record was created
        constexpr const char* CREATED_AT = "created_at";
        ///< Device identifier that admitted this patient (e.g., ICU-MON-04)
        constexpr const char* DEVICE_LABEL = "device_label";
        ///< Unix milliseconds timestamp when patient was discharged (NULL if currently admitted)
        constexpr const char* DISCHARGED_AT = "discharged_at";
        ///< Date of birth (ISO 8601 format: YYYY-MM-DD)
        constexpr const char* DOB = "dob";
        ///< Unix milliseconds timestamp of last HIS lookup (for caching)
        constexpr const char* LAST_LOOKUP_AT = "last_lookup_at";
        ///< Source of last lookup (HIS/Mock/Manual)
        constexpr const char* LOOKUP_SOURCE = "lookup_source";
        ///< Medical Record Number (unique patient identifier, primary key)
        constexpr const char* MRN = "mrn";
        ///< Patient full name
        constexpr const char* NAME = "name";
        ///< Legacy room/bed assignment (deprecated, use bed_location instead)
        constexpr const char* ROOM = "room";
        ///< Biological sex (M/F/O/U)
        constexpr const char* SEX = "sex";
    }

    /**
     * @namespace PredictiveScores
     * @brief Column names for predictive_scores table.
     *
     * Predictive analytics outputs and provenance
     */
    namespace PredictiveScores {
        ///< details
        constexpr const char* DETAILS = "details";
        ///< id
        constexpr const char* ID = "id";
        ///< model_version
        constexpr const char* MODEL_VERSION = "model_version";
        ///< patient_id
        constexpr const char* PATIENT_ID = "patient_id";
        ///< score_type
        constexpr const char* SCORE_TYPE = "score_type";
        ///< score_value
        constexpr const char* SCORE_VALUE = "score_value";
        ///< timestamp
        constexpr const char* TIMESTAMP = "timestamp";
    }

    /**
     * @namespace SecurityAuditLog
     * @brief Column names for security_audit_log table.
     *
     * Security-relevant events for audit, forensics, and compliance with hash chain for tamper detection
     */
    namespace SecurityAuditLog {
        ///< Additional event details (JSON)
        constexpr const char* DETAILS = "details";
        ///< Device identifier
        constexpr const char* DEVICE_ID = "device_id";
        ///< Error code if success is false
        constexpr const char* ERROR_CODE = "error_code";
        ///< Error message if success is false
        constexpr const char* ERROR_MESSAGE = "error_message";
        ///< Event category (authentication/connection/data_transmission/certificate/authorization/security_config)
        constexpr const char* EVENT_CATEGORY = "event_category";
        ///< Event type (authentication, connection, data_transmission, etc.)
        constexpr const char* EVENT_TYPE = "event_type";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< SHA-256 hash of previous entry (hash chain for tamper detection)
        constexpr const char* PREVIOUS_HASH = "previous_hash";
        ///< Severity level (info/warning/error/critical)
        constexpr const char* SEVERITY = "severity";
        ///< Source IP address
        constexpr const char* SOURCE_IP = "source_ip";
        ///< Whether operation succeeded
        constexpr const char* SUCCESS = "success";
        ///< Unix milliseconds timestamp
        constexpr const char* TIMESTAMP = "timestamp";
        ///< User ID (if applicable)
        constexpr const char* USER_ID = "user_id";
    }

    /**
     * @namespace Settings
     * @brief Column names for settings table.
     *
     * Device configuration settings and user preferences
     */
    namespace Settings {
        ///< Setting key (e.g., deviceId, deviceLabel, measurementUnit)
        constexpr const char* KEY = "key";
        ///< Unix milliseconds timestamp when setting was last updated
        constexpr const char* UPDATED_AT = "updated_at";
        ///< User ID who updated the setting (NULL if system/default)
        constexpr const char* UPDATED_BY = "updated_by";
        ///< Setting value
        constexpr const char* VALUE = "value";
    }

    /**
     * @namespace Snapshots
     * @brief Column names for snapshots table.
     *
     * Captured waveform or contextual snapshot data (blobs) with metadata
     */
    namespace Snapshots {
        ///< capture_time
        constexpr const char* CAPTURE_TIME = "capture_time";
        ///< checksum
        constexpr const char* CHECKSUM = "checksum";
        ///< compression
        constexpr const char* COMPRESSION = "compression";
        ///< data
        constexpr const char* DATA = "data";
        ///< encoding
        constexpr const char* ENCODING = "encoding";
        ///< length
        constexpr const char* LENGTH = "length";
        ///< patient_id
        constexpr const char* PATIENT_ID = "patient_id";
        ///< sample_rate
        constexpr const char* SAMPLE_RATE = "sample_rate";
        ///< snapshot_id
        constexpr const char* SNAPSHOT_ID = "snapshot_id";
        ///< waveform_type
        constexpr const char* WAVEFORM_TYPE = "waveform_type";
    }

    /**
     * @namespace TelemetryMetrics
     * @brief Column names for telemetry_metrics table.
     *
     * Telemetry transmission timing and performance metrics for benchmarking and diagnostics
     */
    namespace TelemetryMetrics {
        ///< Timestamp when batch object was created (Unix milliseconds)
        constexpr const char* BATCH_CREATED_AT = "batch_created_at";
        ///< Computed: batch_created_at - data_created_at
        constexpr const char* BATCH_CREATION_LATENCY_MS = "batch_creation_latency_ms";
        ///< Unique batch identifier (UUID, referenced by vitals.batch_id)
        constexpr const char* BATCH_ID = "batch_id";
        ///< Size of raw payload in bytes
        constexpr const char* BATCH_SIZE_BYTES = "batch_size_bytes";
        ///< Size after compression (if applicable)
        constexpr const char* COMPRESSED_SIZE_BYTES = "compressed_size_bytes";
        ///< Timestamp when metrics record was created (Unix milliseconds)
        constexpr const char* CREATED_AT = "created_at";
        ///< Timestamp of oldest data point in batch (Unix milliseconds)
        constexpr const char* DATA_CREATED_AT = "data_created_at";
        ///< Device serial number
        constexpr const char* DEVICE_ID = "device_id";
        ///< Device asset tag
        constexpr const char* DEVICE_LABEL = "device_label";
        ///< Computed: server_ack_at - data_created_at
        constexpr const char* END_TO_END_LATENCY_MS = "end_to_end_latency_ms";
        ///< Error message if status is failed
        constexpr const char* ERROR_MESSAGE = "error_message";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< Latency classification (excellent/good/acceptable/slow)
        constexpr const char* LATENCY_CLASS = "latency_class";
        ///< Computed: server_received_at - transmitted_at
        constexpr const char* NETWORK_LATENCY_MS = "network_latency_ms";
        ///< Patient MRN (NULL if no patient admitted)
        constexpr const char* PATIENT_MRN = "patient_mrn";
        ///< Computed: transmitted_at - queued_for_tx_at
        constexpr const char* QUEUE_WAIT_LATENCY_MS = "queue_wait_latency_ms";
        ///< Timestamp when batch was queued for transmission (Unix milliseconds)
        constexpr const char* QUEUED_FOR_TX_AT = "queued_for_tx_at";
        ///< Number of records in batch
        constexpr const char* RECORD_COUNT = "record_count";
        ///< Number of retry attempts
        constexpr const char* RETRY_COUNT = "retry_count";
        ///< Timestamp when server sent acknowledgment (Unix milliseconds)
        constexpr const char* SERVER_ACK_AT = "server_ack_at";
        ///< Timestamp when server processed batch (Unix milliseconds)
        constexpr const char* SERVER_PROCESSED_AT = "server_processed_at";
        ///< Computed: server_processed_at - server_received_at
        constexpr const char* SERVER_PROCESSING_LATENCY_MS = "server_processing_latency_ms";
        ///< Timestamp when server received batch (Unix milliseconds)
        constexpr const char* SERVER_RECEIVED_AT = "server_received_at";
        ///< Timestamp when batch was digitally signed (Unix milliseconds)
        constexpr const char* SIGNED_AT = "signed_at";
        ///< Computed: signed_at - batch_created_at
        constexpr const char* SIGNING_LATENCY_MS = "signing_latency_ms";
        ///< Status (success/failed/timeout/retrying)
        constexpr const char* STATUS = "status";
        ///< Timestamp when transmission started (Unix milliseconds)
        constexpr const char* TRANSMITTED_AT = "transmitted_at";
        ///< Timestamp when metrics record was last updated (Unix milliseconds)
        constexpr const char* UPDATED_AT = "updated_at";
    }

    /**
     * @namespace Users
     * @brief Column names for users table.
     *
     * Local device accounts (PIN-based metadata) used for audit and RBAC
     */
    namespace Users {
        ///< Unix milliseconds timestamp when user was created
        constexpr const char* CREATED_AT = "created_at";
        ///< Unix milliseconds timestamp of last login
        constexpr const char* LAST_LOGIN = "last_login";
        ///< Hashed PIN (never store plaintext)
        constexpr const char* PIN_HASH = "pin_hash";
        ///< User role (NURSE, PHYSICIAN, TECHNICIAN, ADMINISTRATOR, OBSERVER)
        constexpr const char* ROLE = "role";
        ///< User identifier
        constexpr const char* USER_ID = "user_id";
        ///< Username (unique)
        constexpr const char* USERNAME = "username";
    }

    /**
     * @namespace Vitals
     * @brief Column names for vitals table.
     *
     * Vital signs time-series data. Largest table with high-frequency writes.
     */
    namespace Vitals {
        ///< Telemetry batch ID (FK to telemetry_metrics.batch_id)
        constexpr const char* BATCH_ID = "batch_id";
        ///< Device serial number
        constexpr const char* DEVICE_ID = "device_id";
        ///< Device asset tag (e.g., ICU-MON-04)
        constexpr const char* DEVICE_LABEL = "device_label";
        ///< Heart rate in BPM
        constexpr const char* HEART_RATE = "heart_rate";
        ///< Primary key
        constexpr const char* ID = "id";
        ///< Whether data has been synced to server
        constexpr const char* IS_SYNCED = "is_synced";
        ///< DEPRECATED: Use patient_mrn instead
        constexpr const char* PATIENT_ID = "patient_id";
        ///< Patient MRN (FK to patients.mrn, REQUIRED for patient association)
        constexpr const char* PATIENT_MRN = "patient_mrn";
        ///< Respiration rate in breaths per minute
        constexpr const char* RESPIRATION_RATE = "respiration_rate";
        ///< Sample rate in Hz
        constexpr const char* SAMPLE_RATE_HZ = "sample_rate_hz";
        ///< Signal quality (0-100)
        constexpr const char* SIGNAL_QUALITY = "signal_quality";
        ///< Data source (Simulator/Device/Test)
        constexpr const char* SOURCE = "source";
        ///< SpO2 oxygen saturation (0-100%)
        constexpr const char* SPO2 = "spo2";
        ///< Unix milliseconds when data was measured
        constexpr const char* TIMESTAMP = "timestamp";
        ///< ISO 8601 timestamp (for readability)
        constexpr const char* TIMESTAMP_ISO = "timestamp_iso";
        ///< UUID for distributed systems
        constexpr const char* UUID = "uuid";
    }
}

/**
 * @namespace Constraints
 * @brief Constraint definitions for validation.
 */
namespace Constraints {

    namespace ActionLog {
        // Valid values for result: SUCCESS, FAILURE, PARTIAL
    }

    namespace AdmissionEvents {
        // Valid values for event_type: admission, discharge, transfer
    }

    namespace Alarms {
        // Valid values for priority: CRITICAL, HIGH, MEDIUM, LOW
    }

    namespace Certificates {
        // Valid values for status: active, expired, revoked, pending_renewal
    }

    namespace Patients {
        // Valid values for sex: M, F, O, U
        // Valid values for admission_source: manual, barcode, central_station, emergency, transfer
        // Valid values for admission_status: ADMITTED, DISCHARGED, TRANSFERRED
    }

    namespace SecurityAuditLog {
        // Valid values for severity: info, warning, error, critical
    }

    namespace TelemetryMetrics {
        // Valid values for status: success, failed, timeout, retrying
        // Valid values for latency_class: excellent, good, acceptable, slow
    }

    namespace Users {
        // Valid values for role: NURSE, PHYSICIAN, TECHNICIAN, ADMINISTRATOR, OBSERVER
    }

    namespace Vitals {
    }
}

} // namespace Schema

#endif // SCHEMAINFO_H
